sudo: false
language: node_js
node_js: 10
#cache: npm
#  directories: node_modules
branches:
  only: blogging  # build blogging branch only
#install: npm install
before_script:
  - git config user.name "wgdzlh"
  - git config user.email "13d1101@gmail.com"
  - sed -i'' "s~https://github.com/wgdzlh/wgdzlh.github.io.git~https://${GH_TOKEN}@github.com/wgdzlh/wgdzlh.github.io.git~" _config.yml
script:
  - hexo g  # generate static files
  - hexo d

